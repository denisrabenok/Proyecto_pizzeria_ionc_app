
export interface IngredientesPromo{
    id: string,
    nombre:string,
    descripcion:string,
    imagenUrl:string,
    checked : boolean;  // variable que controla si ha sido marcado o no
    seleccion : boolean;
  }